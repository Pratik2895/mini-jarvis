# Core LLM - google-genai (new SDK, replaces deprecated google-generativeai)
google-genai>=2.20.0

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.32

# Voice - STT (local, no API key needed)
faster-whisper>=1.1.0

# Voice - TTS (offline)
pyttsx3>=2.90

# Audio capture
sounddevice>=0.5.0
soundfile>=0.12.0
numpy>=1.26.0

# HTTP client
httpx>=0.27.0
requests>=2.32.0

# Settings / env
python-dotenv>=1.0.0
pydantic-settings>=2.5.0

# Timezone data (required on Windows)
tzdata>=2026.0

# Databricks SDK (for local client -> serving endpoint)
databricks-sdk>=0.36.0

# Dev / test
pytest>=8.3.0
pytest-asyncio>=0.24.0
